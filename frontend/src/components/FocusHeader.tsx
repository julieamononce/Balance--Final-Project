import React from "react";

export default function FocusHeader() {
    return (
    <header className="w-full p-4 border-b border-gray-200 shadow-sm flex items-center justify-between">
        <h1 className="text-lg font-semibold">Focus Mode</h1>
        <div className="text-sm text-gray-500">Deep Work Session</div>
    </header>
    );
}
